# AI Agent Instructions

- **GitHub Workflows:** NEVER modify, edit, or delete anything in the `.github/workflows/` directory. The user has set up an automated CI/CD workflow here.
- **Dependencies:** NEVER delete or remove existing dependencies from `package.json`. You may add new dependencies if explicitly needed, but do not touch existing ones.
